// Code generated by kgen crud. DO NOT EDIT.

// Package custom_variable_override implements the Terraform resource and data source for custom_variable_override.
package custom_variable_override

import (
	"context"
	"encoding/json"
	"fmt"
	"strconv"
	"strings"
	"time"

	"github.com/go-faster/jx"
	"github.com/hashicorp/terraform-plugin-framework/diag"
	"github.com/hashicorp/terraform-plugin-framework/path"
	"github.com/hashicorp/terraform-plugin-framework/resource"
	"github.com/hashicorp/terraform-plugin-framework/types"
	generated "github.com/kionsoftware/kion-sdk-go/generated/v3_16"

	"terraform-provider-kion/internal/errs"
	"terraform-provider-kion/internal/flex"
	"terraform-provider-kion/internal/framework"
)

// Resource name constant.
const (
	ResNameCustomVariableOverride = "Custom Variable Override"
)

// Ensure the implementation satisfies the expected interfaces.
var (
	_ resource.Resource                = &customVariableOverrideResource{}
	_ resource.ResourceWithConfigure   = &customVariableOverrideResource{}
	_ resource.ResourceWithImportState = &customVariableOverrideResource{}
)

// NewCustomVariableOverrideResource returns a new instance of the resource.
func NewCustomVariableOverrideResource() resource.Resource {
	return &customVariableOverrideResource{}
}

type customVariableOverrideResource struct {
	framework.ResourceWithConfigure
}

func (r *customVariableOverrideResource) Metadata(_ context.Context, req resource.MetadataRequest, resp *resource.MetadataResponse) {
	resp.TypeName = req.ProviderTypeName + "_custom_variable_override"
}

func (r *customVariableOverrideResource) Schema(ctx context.Context, _ resource.SchemaRequest, resp *resource.SchemaResponse) {
	// Schema comes from custom_variable_override_schema_gen.go, generated from the OpenAPI spec
	// plus codegen/schema_overrides.yaml. It was previously duplicated here by
	// hand, so the generated file was dead code and the two could drift without
	// anything failing.
	resp.Schema = CustomVariableOverrideResourceSchema(ctx)
}

func (r *customVariableOverrideResource) Create(ctx context.Context, req resource.CreateRequest, resp *resource.CreateResponse) {
	conn := r.Meta().Client

	var plan customVariableOverrideResourceModel
	resp.Diagnostics.Append(req.Plan.Get(ctx, &plan)...)
	if resp.Diagnostics.HasError() {
		return
	}

	entityType := plan.EntityType.ValueString()
	entityID, parseDiags := parseIDString(plan.EntityID.ValueString(), "entity_id")
	resp.Diagnostics.Append(parseDiags...)
	if resp.Diagnostics.HasError() {
		return
	}
	cvID, parseDiags := parseIDString(plan.CustomVariableID.ValueString(), "custom_variable_id")
	resp.Diagnostics.Append(parseDiags...)
	if resp.Diagnostics.HasError() {
		return
	}

	rawValue, valueDiags := buildOverrideValue(ctx, &plan)
	resp.Diagnostics.Append(valueDiags...)
	if resp.Diagnostics.HasError() {
		return
	}

	createDiags := createOrUpdateOverride(ctx, conn, entityType, entityID, cvID, rawValue)
	resp.Diagnostics.Append(createDiags...)
	if resp.Diagnostics.HasError() {
		return
	}

	// Match the old provider's id format: "{entity_type}/{entity_id}/{custom_variable_id}"
	plan.ID = types.StringValue(fmt.Sprintf("%s/%s/%s", entityType, plan.EntityID.ValueString(), plan.CustomVariableID.ValueString()))
	plan.LastUpdated = types.StringValue(time.Now().Format(time.RFC850))

	// Read back the resource to populate state accurately.
	readDiags := readOverrideIntoModel(ctx, conn, &plan)
	resp.Diagnostics.Append(readDiags...)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &plan)...)
}

func (r *customVariableOverrideResource) Read(ctx context.Context, req resource.ReadRequest, resp *resource.ReadResponse) {
	conn := r.Meta().Client

	var state customVariableOverrideResourceModel
	resp.Diagnostics.Append(req.State.Get(ctx, &state)...)
	if resp.Diagnostics.HasError() {
		return
	}

	readDiags := readOverrideIntoModel(ctx, conn, &state)
	resp.Diagnostics.Append(readDiags...)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &state)...)
}

func (r *customVariableOverrideResource) Update(ctx context.Context, req resource.UpdateRequest, resp *resource.UpdateResponse) {
	conn := r.Meta().Client

	var plan customVariableOverrideResourceModel
	resp.Diagnostics.Append(req.Plan.Get(ctx, &plan)...)
	if resp.Diagnostics.HasError() {
		return
	}

	entityType := plan.EntityType.ValueString()
	entityID, parseDiags := parseIDString(plan.EntityID.ValueString(), "entity_id")
	resp.Diagnostics.Append(parseDiags...)
	if resp.Diagnostics.HasError() {
		return
	}
	cvID, parseDiags := parseIDString(plan.CustomVariableID.ValueString(), "custom_variable_id")
	resp.Diagnostics.Append(parseDiags...)
	if resp.Diagnostics.HasError() {
		return
	}

	rawValue, valueDiags := buildOverrideValue(ctx, &plan)
	resp.Diagnostics.Append(valueDiags...)
	if resp.Diagnostics.HasError() {
		return
	}

	updateDiags := createOrUpdateOverride(ctx, conn, entityType, entityID, cvID, rawValue)
	resp.Diagnostics.Append(updateDiags...)
	if resp.Diagnostics.HasError() {
		return
	}

	plan.LastUpdated = types.StringValue(time.Now().Format(time.RFC850))

	readDiags := readOverrideIntoModel(ctx, conn, &plan)
	resp.Diagnostics.Append(readDiags...)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &plan)...)
}

func (r *customVariableOverrideResource) Delete(ctx context.Context, req resource.DeleteRequest, resp *resource.DeleteResponse) {
	conn := r.Meta().Client

	var state customVariableOverrideResourceModel
	resp.Diagnostics.Append(req.State.Get(ctx, &state)...)
	if resp.Diagnostics.HasError() {
		return
	}

	entityType := state.EntityType.ValueString()
	entityID, parseDiags := parseIDString(state.EntityID.ValueString(), "entity_id")
	resp.Diagnostics.Append(parseDiags...)
	if resp.Diagnostics.HasError() {
		return
	}
	cvID, parseDiags := parseIDString(state.CustomVariableID.ValueString(), "custom_variable_id")
	resp.Diagnostics.Append(parseDiags...)
	if resp.Diagnostics.HasError() {
		return
	}

	switch entityType {
	case "project":
		out, err := conn.DeleteProjectCustomVariable(ctx, generated.DeleteProjectCustomVariableParams{
			ProjectID:        entityID,
			CustomVariableID: cvID,
		})
		if err != nil {
			resp.Diagnostics.AddError(fmt.Sprintf("deleting %s", ResNameCustomVariableOverride), err.Error())
			return
		}
		if errs.IsNotFound(out) {
			return
		}
		resp.Diagnostics.Append(errs.ResponseDiagnostics(fmt.Sprintf("deleting %s", ResNameCustomVariableOverride), out)...)
	case "ou":
		out, err := conn.DeleteOUCustomVariable(ctx, generated.DeleteOUCustomVariableParams{
			OuID:             entityID,
			CustomVariableID: cvID,
		})
		if err != nil {
			resp.Diagnostics.AddError(fmt.Sprintf("deleting %s", ResNameCustomVariableOverride), err.Error())
			return
		}
		if errs.IsNotFound(out) {
			return
		}
		resp.Diagnostics.Append(errs.ResponseDiagnostics(fmt.Sprintf("deleting %s", ResNameCustomVariableOverride), out)...)
	case "account":
		out, err := conn.DeleteAccountCustomVariable(ctx, generated.DeleteAccountCustomVariableParams{
			AccountID:        entityID,
			CustomVariableID: cvID,
		})
		if err != nil {
			resp.Diagnostics.AddError(fmt.Sprintf("deleting %s", ResNameCustomVariableOverride), err.Error())
			return
		}
		if errs.IsNotFound(out) {
			return
		}
		resp.Diagnostics.Append(errs.ResponseDiagnostics(fmt.Sprintf("deleting %s", ResNameCustomVariableOverride), out)...)
	default:
		resp.Diagnostics.AddError(
			fmt.Sprintf("deleting %s", ResNameCustomVariableOverride),
			fmt.Sprintf("unsupported entity_type %q; expected one of: project, ou, account", entityType),
		)
	}
}

// parseIDString converts a string ID field (entity_id, custom_variable_id) to int64.
func parseIDString(s, field string) (int64, diag.Diagnostics) {
	var diags diag.Diagnostics
	id, err := strconv.ParseInt(s, 10, 64)
	if err != nil {
		diags.AddError(
			fmt.Sprintf("invalid %s", field),
			fmt.Sprintf("unable to parse %s %q as int64: %s", field, s, err.Error()),
		)
		return 0, diags
	}
	return id, diags
}

// buildOverrideValue assembles a jx.Raw JSON value from whichever of value_string,
// value_list, or value_map is set on the model.
func buildOverrideValue(ctx context.Context, model *customVariableOverrideResourceModel) (jx.Raw, diag.Diagnostics) {
	var diags diag.Diagnostics

	stringSet := !model.ValueString.IsNull() && !model.ValueString.IsUnknown()
	listSet := !model.ValueList.IsNull() && !model.ValueList.IsUnknown()
	mapSet := !model.ValueMap.IsNull() && !model.ValueMap.IsUnknown()

	set := 0
	if stringSet {
		set++
	}
	if listSet {
		set++
	}
	if mapSet {
		set++
	}
	if set == 0 {
		diags.AddError(
			fmt.Sprintf("building %s value", ResNameCustomVariableOverride),
			"one of value_string, value_list, or value_map must be set",
		)
		return nil, diags
	}
	if set > 1 {
		diags.AddError(
			fmt.Sprintf("building %s value", ResNameCustomVariableOverride),
			"only one of value_string, value_list, or value_map may be set",
		)
		return nil, diags
	}

	switch {
	case stringSet:
		b, err := json.Marshal(model.ValueString.ValueString())
		if err != nil {
			diags.AddError(fmt.Sprintf("building %s value", ResNameCustomVariableOverride), err.Error())
			return nil, diags
		}
		return jx.Raw(b), diags
	case listSet:
		items, d := flex.StringSliceFromFramework(ctx, model.ValueList)
		diags.Append(d...)
		if diags.HasError() {
			return nil, diags
		}
		if items == nil {
			items = []string{}
		}
		b, err := json.Marshal(items)
		if err != nil {
			diags.AddError(fmt.Sprintf("building %s value", ResNameCustomVariableOverride), err.Error())
			return nil, diags
		}
		return jx.Raw(b), diags
	case mapSet:
		var elems map[string]string
		d := model.ValueMap.ElementsAs(ctx, &elems, false)
		diags.Append(d...)
		if diags.HasError() {
			return nil, diags
		}
		if elems == nil {
			elems = map[string]string{}
		}
		b, err := json.Marshal(elems)
		if err != nil {
			diags.AddError(fmt.Sprintf("building %s value", ResNameCustomVariableOverride), err.Error())
			return nil, diags
		}
		return jx.Raw(b), diags
	}

	return nil, diags
}

// createOrUpdateOverride dispatches to the correct SDK method based on entity_type.
func createOrUpdateOverride(ctx context.Context, conn *generated.Client, entityType string, entityID, cvID int64, rawValue jx.Raw) diag.Diagnostics {
	var diags diag.Diagnostics

	switch entityType {
	case "project":
		input := generated.OptProjectCustomVariableCreateOrUpdate{
			Value: generated.ProjectCustomVariableCreateOrUpdate{Value: rawValue},
			Set:   true,
		}
		out, err := conn.CreateOrUpdateProjectCustomVariable(ctx, input, generated.CreateOrUpdateProjectCustomVariableParams{
			ProjectID:        entityID,
			CustomVariableID: cvID,
		})
		if err != nil {
			diags.AddError(fmt.Sprintf("creating/updating %s", ResNameCustomVariableOverride), err.Error())
			return diags
		}
		diags.Append(errs.ResponseDiagnostics(fmt.Sprintf("creating/updating %s", ResNameCustomVariableOverride), out)...)
	case "ou":
		input := generated.OptOUCustomVariableCreateOrUpdate{
			Value: generated.OUCustomVariableCreateOrUpdate{Value: rawValue},
			Set:   true,
		}
		out, err := conn.CreateOrUpdateOUCustomVariable(ctx, input, generated.CreateOrUpdateOUCustomVariableParams{
			OuID:             entityID,
			CustomVariableID: cvID,
		})
		if err != nil {
			diags.AddError(fmt.Sprintf("creating/updating %s", ResNameCustomVariableOverride), err.Error())
			return diags
		}
		diags.Append(errs.ResponseDiagnostics(fmt.Sprintf("creating/updating %s", ResNameCustomVariableOverride), out)...)
	case "account":
		input := generated.OptAccountCustomVariableCreateOrUpdate{
			Value: generated.AccountCustomVariableCreateOrUpdate{Value: rawValue},
			Set:   true,
		}
		out, err := conn.CreateOrUpdateAccountCustomVariable(ctx, input, generated.CreateOrUpdateAccountCustomVariableParams{
			AccountID:        entityID,
			CustomVariableID: cvID,
		})
		if err != nil {
			diags.AddError(fmt.Sprintf("creating/updating %s", ResNameCustomVariableOverride), err.Error())
			return diags
		}
		diags.Append(errs.ResponseDiagnostics(fmt.Sprintf("creating/updating %s", ResNameCustomVariableOverride), out)...)
	default:
		diags.AddError(
			fmt.Sprintf("creating/updating %s", ResNameCustomVariableOverride),
			fmt.Sprintf("unsupported entity_type %q; expected one of: project, ou, account", entityType),
		)
	}

	return diags
}

// readOverrideIntoModel fetches the override and flattens it into the model.
func readOverrideIntoModel(ctx context.Context, conn *generated.Client, model *customVariableOverrideResourceModel) diag.Diagnostics {
	var diags diag.Diagnostics

	entityType := model.EntityType.ValueString()
	entityID, d := parseIDString(model.EntityID.ValueString(), "entity_id")
	diags.Append(d...)
	if diags.HasError() {
		return diags
	}
	cvID, d := parseIDString(model.CustomVariableID.ValueString(), "custom_variable_id")
	diags.Append(d...)
	if diags.HasError() {
		return diags
	}

	var out any
	switch entityType {
	case "project":
		res, err := conn.GetProjectCustomVariable(ctx, generated.GetProjectCustomVariableParams{
			ProjectID:        entityID,
			CustomVariableID: cvID,
		})
		if err != nil {
			diags.AddError(fmt.Sprintf("reading %s", ResNameCustomVariableOverride), err.Error())
			return diags
		}
		out = res
	case "ou":
		res, err := conn.GetOUCustomVariable(ctx, generated.GetOUCustomVariableParams{
			OuID:             entityID,
			CustomVariableID: cvID,
		})
		if err != nil {
			diags.AddError(fmt.Sprintf("reading %s", ResNameCustomVariableOverride), err.Error())
			return diags
		}
		out = res
	case "account":
		res, err := conn.GetAccountCustomVariable(ctx, generated.GetAccountCustomVariableParams{
			AccountID:        entityID,
			CustomVariableID: cvID,
		})
		if err != nil {
			diags.AddError(fmt.Sprintf("reading %s", ResNameCustomVariableOverride), err.Error())
			return diags
		}
		out = res
	default:
		diags.AddError(
			fmt.Sprintf("reading %s", ResNameCustomVariableOverride),
			fmt.Sprintf("unsupported entity_type %q; expected one of: project, ou, account", entityType),
		)
		return diags
	}

	diags.Append(flattenCustomVariableOverride(out, model)...)
	return diags
}

// ImportState splits the three-part id Create builds,
// "<entity_type>/<entity_id>/<custom_variable_id>". All three parts are
// required: entity_type selects which endpoint Read calls, and neither id can
// be derived from the other. Setting only "id" left Read with no parent and no
// variable, so every import failed.
func (r *customVariableOverrideResource) ImportState(ctx context.Context, req resource.ImportStateRequest, resp *resource.ImportStateResponse) {
	parts := strings.Split(req.ID, "/")
	if len(parts) != 3 || parts[0] == "" || parts[1] == "" || parts[2] == "" {
		resp.Diagnostics.AddError(
			fmt.Sprintf("importing %s", ResNameCustomVariableOverride),
			fmt.Sprintf("expected an id of the form \"<entity_type>/<entity_id>/<custom_variable_id>\", got %q", req.ID),
		)
		return
	}
	entityType, entityID, cvID := parts[0], parts[1], parts[2]

	switch entityType {
	case "project", "ou", "account":
	default:
		resp.Diagnostics.AddError(
			fmt.Sprintf("importing %s", ResNameCustomVariableOverride),
			fmt.Sprintf("unsupported entity_type %q; expected one of: project, ou, account", entityType),
		)
		return
	}

	_, entityDiags := parseIDString(entityID, "entity_id")
	resp.Diagnostics.Append(entityDiags...)
	_, cvDiags := parseIDString(cvID, "custom_variable_id")
	resp.Diagnostics.Append(cvDiags...)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.SetAttribute(ctx, path.Root("id"), req.ID)...)
	resp.Diagnostics.Append(resp.State.SetAttribute(ctx, path.Root("entity_type"), entityType)...)
	resp.Diagnostics.Append(resp.State.SetAttribute(ctx, path.Root("entity_id"), entityID)...)
	resp.Diagnostics.Append(resp.State.SetAttribute(ctx, path.Root("custom_variable_id"), cvID)...)
}

func flattenCustomVariableOverride(apiObject any, model *customVariableOverrideResourceModel) diag.Diagnostics {
	var diags diag.Diagnostics

	resp, ok := apiObject.(*generated.CustomVariableOverrideResponse)
	if !ok {
		return errs.ResponseDiagnostics("reading "+ResNameCustomVariableOverride, apiObject)
	}

	if !resp.Data.IsSet() {
		return diags
	}

	data := resp.Data.Value
	cvType := data.CustomVariableType.Value

	// Determine which raw JSON to read: prefer the override if set, otherwise
	// fall back to the effective value.
	var raw jx.Raw
	if data.Override.IsSet() && len(data.Override.Value.Value) > 0 {
		raw = data.Override.Value.Value
	} else if len(data.Value) > 0 {
		raw = data.Value
	}

	if len(raw) == 0 {
		// No value present; leave model fields as-is.
		return diags
	}

	switch cvType {
	case "string":
		var s string
		if err := json.Unmarshal(raw, &s); err != nil {
			diags.AddError(
				fmt.Sprintf("reading %s", ResNameCustomVariableOverride),
				fmt.Sprintf("failed to unmarshal string value: %s", err.Error()),
			)
			return diags
		}
		model.ValueString = types.StringValue(s)
	case "list":
		var items []string
		if err := json.Unmarshal(raw, &items); err != nil {
			diags.AddError(
				fmt.Sprintf("reading %s", ResNameCustomVariableOverride),
				fmt.Sprintf("failed to unmarshal list value: %s", err.Error()),
			)
			return diags
		}
		listVal, d := flex.StringSliceToFramework(context.Background(), items)
		diags.Append(d...)
		if diags.HasError() {
			return diags
		}
		model.ValueList = listVal
	case "map":
		var m map[string]string
		if err := json.Unmarshal(raw, &m); err != nil {
			diags.AddError(
				fmt.Sprintf("reading %s", ResNameCustomVariableOverride),
				fmt.Sprintf("failed to unmarshal map value: %s", err.Error()),
			)
			return diags
		}
		elems := make(map[string]types.String, len(m))
		for k, v := range m {
			elems[k] = types.StringValue(v)
		}
		mapVal, d := types.MapValueFrom(context.Background(), types.StringType, elems)
		diags.Append(d...)
		if diags.HasError() {
			return diags
		}
		model.ValueMap = mapVal
	}

	return diags
}

type customVariableOverrideResourceModel struct {
	ID               types.String `tfsdk:"id"`
	CustomVariableID types.String `tfsdk:"custom_variable_id"`
	EntityID         types.String `tfsdk:"entity_id"`
	EntityType       types.String `tfsdk:"entity_type"`
	LastUpdated      types.String `tfsdk:"last_updated"`
	ValueList        types.List   `tfsdk:"value_list"`
	ValueMap         types.Map    `tfsdk:"value_map"`
	ValueString      types.String `tfsdk:"value_string"`
}
