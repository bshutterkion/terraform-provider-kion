// Code generated by kgen crud. DO NOT EDIT.

// Package webhook implements the Terraform resource and data source for webhook.
package webhook

import (
	"context"
	"fmt"
	"strconv"

	"github.com/hashicorp/terraform-plugin-framework/diag"
	"github.com/hashicorp/terraform-plugin-framework/path"
	"github.com/hashicorp/terraform-plugin-framework/resource"
	"github.com/hashicorp/terraform-plugin-framework/types"
	generated "github.com/kionsoftware/kion-sdk-go/generated/v3_16"

	"terraform-provider-kion/internal/errs"
	"terraform-provider-kion/internal/flex"
	"terraform-provider-kion/internal/framework"
)

const ResNameWebhook = "Webhook"

var (
	_ resource.Resource                = &webhookResource{}
	_ resource.ResourceWithConfigure   = &webhookResource{}
	_ resource.ResourceWithImportState = &webhookResource{}
)

// NewWebhookResource returns a new instance of the resource.
func NewWebhookResource() resource.Resource {
	return &webhookResource{}
}

type webhookResource struct {
	framework.ResourceWithConfigure
}

func (r *webhookResource) Metadata(_ context.Context, req resource.MetadataRequest, resp *resource.MetadataResponse) {
	resp.TypeName = req.ProviderTypeName + "_webhook"
}

func (r *webhookResource) Schema(ctx context.Context, _ resource.SchemaRequest, resp *resource.SchemaResponse) {
	// Schema is generated from the OpenAPI spec (webhook_schema_gen.go); the
	// bits the spec can't express come from codegen/schema_overrides.yaml.
	resp.Schema = WebhookResourceSchema(ctx)
	resp.Schema.Version = 1
}

func (r *webhookResource) Create(ctx context.Context, req resource.CreateRequest, resp *resource.CreateResponse) {
	conn := r.Meta().Client

	var plan WebhookModel
	resp.Diagnostics.Append(req.Plan.Get(ctx, &plan)...)
	if resp.Diagnostics.HasError() {
		return
	}

	ownerUserGroupIds, ownerUserGroupIdsDiags := flex.Uint64SliceFromFramework(ctx, plan.OwnerUserGroupIds)
	resp.Diagnostics.Append(ownerUserGroupIdsDiags...)
	ownerUserIds, ownerUserIdsDiags := flex.Uint64SliceFromFramework(ctx, plan.OwnerUserIds)
	resp.Diagnostics.Append(ownerUserIdsDiags...)
	if resp.Diagnostics.HasError() {
		return
	}

	input := &generated.WebhookCreate{
		CalloutURL:           flex.StringValueFromFramework(plan.CalloutUrl),
		Description:          flex.OptStringFromFramework(plan.Description),
		Name:                 flex.StringValueFromFramework(plan.Name),
		RequestBody:          flex.OptStringFromFramework(plan.RequestBody),
		RequestHeaders:       flex.OptStringFromFramework(plan.RequestHeaders),
		RequestMethod:        flex.OptStringFromFramework(plan.RequestMethod),
		ShouldSendSecureInfo: flex.OptNilBoolFromFramework(plan.ShouldSendSecureInfo),
		SkipSsl:              flex.OptNilBoolFromFramework(plan.SkipSsl),
		TimeoutInSeconds:     flex.Int64ValueFromFramework(plan.TimeoutInSeconds),
		UseRequestHeaders:    flex.OptNilBoolFromFramework(plan.UseRequestHeaders),
		OwnerUserGroupIds:    generated.OptNilUint64Array{Value: ownerUserGroupIds, Set: true},
		OwnerUserIds:         generated.OptNilUint64Array{Value: ownerUserIds, Set: true},
	}

	out, err := conn.PostWebhook(ctx, input)
	if err != nil {
		resp.Diagnostics.AddError(fmt.Sprintf("creating %s", ResNameWebhook), err.Error())
		return
	}

	id, diags := errs.CreatedID(out)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	plan.Id = types.StringValue(strconv.FormatInt(id, 10))

	// Read back the resource to populate computed fields.
	readOut, readErr := conn.GetWebhook(ctx, generated.GetWebhookParams{ID: id})
	if readErr != nil {
		resp.Diagnostics.AddError(fmt.Sprintf("reading %s after creation (ID: %d)", ResNameWebhook, id), readErr.Error())
		return
	}

	resp.Diagnostics.Append(flattenWebhook(ctx, readOut, &plan)...)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &plan)...)
}

func (r *webhookResource) Read(ctx context.Context, req resource.ReadRequest, resp *resource.ReadResponse) {
	conn := r.Meta().Client

	var state WebhookModel
	resp.Diagnostics.Append(req.State.Get(ctx, &state)...)
	if resp.Diagnostics.HasError() {
		return
	}

	idInt, err := strconv.ParseInt(state.Id.ValueString(), 10, 64)
	if err != nil {
		resp.Diagnostics.AddError("Invalid ID", err.Error())
		return
	}

	out, err := conn.GetWebhook(ctx, generated.GetWebhookParams{ID: idInt})
	if err != nil {
		resp.Diagnostics.AddError(fmt.Sprintf("reading %s (ID: %d)", ResNameWebhook, idInt), err.Error())
		return
	}

	if errs.IsNotFound(out) {
		resp.State.RemoveResource(ctx)
		return
	}

	resp.Diagnostics.Append(flattenWebhook(ctx, out, &state)...)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &state)...)
}

func (r *webhookResource) Update(ctx context.Context, req resource.UpdateRequest, resp *resource.UpdateResponse) {
	conn := r.Meta().Client

	var plan WebhookModel
	resp.Diagnostics.Append(req.Plan.Get(ctx, &plan)...)
	if resp.Diagnostics.HasError() {
		return
	}

	input := &generated.WebhookUpdate{
		CalloutURL:           flex.OptStringFromFramework(plan.CalloutUrl),
		Description:          flex.OptStringFromFramework(plan.Description),
		Name:                 flex.OptStringFromFramework(plan.Name),
		RequestBody:          flex.OptStringFromFramework(plan.RequestBody),
		RequestHeaders:       flex.OptStringFromFramework(plan.RequestHeaders),
		RequestMethod:        flex.OptStringFromFramework(plan.RequestMethod),
		ShouldSendSecureInfo: flex.OptNilBoolFromFramework(plan.ShouldSendSecureInfo),
		SkipSsl:              flex.OptNilBoolFromFramework(plan.SkipSsl),
		TimeoutInSeconds:     flex.OptInt64FromFramework(plan.TimeoutInSeconds),
		UseRequestHeaders:    flex.OptNilBoolFromFramework(plan.UseRequestHeaders),
	}

	idInt, err := strconv.ParseInt(plan.Id.ValueString(), 10, 64)
	if err != nil {
		resp.Diagnostics.AddError("Invalid ID", err.Error())
		return
	}

	out, err := conn.PatchWebhook(ctx, input, generated.PatchWebhookParams{ID: idInt})
	if err != nil {
		resp.Diagnostics.AddError(fmt.Sprintf("updating %s (ID: %d)", ResNameWebhook, idInt), err.Error())
		return
	}

	diags := errs.ResponseDiagnostics(fmt.Sprintf("updating %s", ResNameWebhook), out)
	resp.Diagnostics.Append(diags...)
	if resp.Diagnostics.HasError() {
		return
	}

	// Read back the resource to get the latest state.
	readOut, readErr := conn.GetWebhook(ctx, generated.GetWebhookParams{ID: idInt})
	if readErr != nil {
		resp.Diagnostics.AddError(fmt.Sprintf("reading %s after update (ID: %d)", ResNameWebhook, idInt), readErr.Error())
		return
	}

	resp.Diagnostics.Append(flattenWebhook(ctx, readOut, &plan)...)
	if resp.Diagnostics.HasError() {
		return
	}

	resp.Diagnostics.Append(resp.State.Set(ctx, &plan)...)
}

func (r *webhookResource) Delete(_ context.Context, _ resource.DeleteRequest, resp *resource.DeleteResponse) {
	// kion_webhook has no delete endpoint. Terraform removes it from state; the
	// resource may continue to exist in Kion.
	resp.Diagnostics.AddWarning(
		"No delete API",
		"kion_webhook has no delete endpoint, so it is removed from Terraform state only and may still exist in Kion.",
	)
}

func (r *webhookResource) ImportState(ctx context.Context, req resource.ImportStateRequest, resp *resource.ImportStateResponse) {
	resp.Diagnostics.Append(resp.State.SetAttribute(ctx, path.Root("id"), req.ID)...)
}

func flattenWebhook(ctx context.Context, apiObject any, model *WebhookModel) diag.Diagnostics {
	var diags diag.Diagnostics
	switch v := apiObject.(type) {
	case *generated.WebhookWithOwnersResponse:
		if v.Data.Set {
			model.CalloutUrl = flex.OptStringToFramework(v.Data.Value.Webhook.Value.CalloutURL)
			model.Description = flex.OptStringToFramework(v.Data.Value.Webhook.Value.Description)
			if v.Data.Value.Webhook.Value.ID.Set {
				model.Id = types.StringValue(strconv.FormatUint(v.Data.Value.Webhook.Value.ID.Value, 10))
			}
			model.Name = flex.OptStringToFramework(v.Data.Value.Webhook.Value.Name)
			model.RequestBody = flex.OptStringToFramework(v.Data.Value.Webhook.Value.RequestBody)
			model.RequestHeaders = flex.OptStringToFramework(v.Data.Value.Webhook.Value.RequestHeaders)
			model.RequestMethod = flex.OptStringToFramework(v.Data.Value.Webhook.Value.RequestMethod)
			model.ShouldSendSecureInfo = flex.OptNilBoolToFramework(v.Data.Value.Webhook.Value.ShouldSendSecureInfo)
			model.SkipSsl = flex.OptNilBoolToFramework(v.Data.Value.Webhook.Value.SkipSsl)
			model.TimeoutInSeconds = flex.OptInt64ToFramework(v.Data.Value.Webhook.Value.TimeoutInSeconds)
			model.UseRequestHeaders = flex.OptNilBoolToFramework(v.Data.Value.Webhook.Value.UseRequestHeaders)
			var ownerUserGroupsIDs []int64
			for _, elem := range v.Data.Value.OwnerUserGroups.Value {
				if elem.ID.Set {
					ownerUserGroupsIDs = append(ownerUserGroupsIDs, int64(elem.ID.Value))
				}
			}
			ownerUserGroupsIDsColl, ownerUserGroupsIDsCD := types.ListValueFrom(ctx, types.Int64Type, ownerUserGroupsIDs)
			diags.Append(ownerUserGroupsIDsCD...)
			model.OwnerUserGroupIds = ownerUserGroupsIDsColl
			var ownerUsersIDs []int64
			for _, elem := range v.Data.Value.OwnerUsers.Value {
				if elem.ID.Set {
					ownerUsersIDs = append(ownerUsersIDs, int64(elem.ID.Value))
				}
			}
			ownerUsersIDsColl, ownerUsersIDsCD := types.ListValueFrom(ctx, types.Int64Type, ownerUsersIDs)
			diags.Append(ownerUsersIDsCD...)
			model.OwnerUserIds = ownerUsersIDsColl
		}
		return diags
	default:
		return errs.ResponseDiagnostics("reading "+ResNameWebhook, apiObject)
	}
}
