// Code generated by kgen crud. DO NOT EDIT.

package gcp_account

import (
	"context"
	"fmt"
	"strings"

	"terraform-provider-kion/internal/conns"

	"github.com/hashicorp/terraform-plugin-testing/helper/resource"

	generated "github.com/kionsoftware/kion-sdk-go/generated/v3_16"
)

func init() {
	resource.AddTestSweepers("kion_gcp_account", &resource.Sweeper{
		Name: "kion_gcp_account",
		F:    sweepGcpAccount,
	})
}

// sweepGcpAccount reads GetAccountIndex (not paginated: one call yields the whole
// collection) collecting ids whose test resources carry the acceptance-test
// prefix, then deletes them via DeleteAccount.
func sweepGcpAccount(_ string) error {
	conn, err := conns.SharedClient()
	if err != nil {
		return fmt.Errorf("getting shared client: %w", err)
	}
	ctx := context.Background()

	var ids []int64
	// GetAccountIndex is not paginated: one call returns the whole collection.
	out, err := conn.Client.GetAccountIndex(ctx, generated.GetAccountIndexParams{})
	if err != nil {
		return fmt.Errorf("listing kion_gcp_account: %w", err)
	}
	if resp, ok := out.(*generated.AccountListResponse); ok {
		items := resp.Data
		for _, item := range items {
			if item.ID.Set && sweepGcpAccountMatch(item) {
				ids = append(ids, int64(item.ID.Value))
			}
		}
	}

	for _, id := range ids {
		if _, err := conn.Client.DeleteAccount(ctx, generated.DeleteAccountParams{ID: id}); err != nil {
			return fmt.Errorf("deleting kion_gcp_account (%d): %w", id, err)
		}
	}
	return nil
}

func sweepGcpAccountMatch(item generated.Account) bool {
	for _, s := range []string{
		item.AccountAlias.Or(""),
		item.AccountName.Or(""),
		item.StartDatecode.Or(""),
	} {
		if strings.HasPrefix(s, "test-acc") {
			return true
		}
	}
	return false
}
