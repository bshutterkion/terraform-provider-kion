// Code generated by kgen crud. DO NOT EDIT.

package label

import (
	"context"
	"fmt"
	"strings"

	"terraform-provider-kion/internal/conns"

	"github.com/hashicorp/terraform-plugin-testing/helper/resource"

	generated "github.com/kionsoftware/kion-sdk-go/generated/v3_16"
)

const sweepPageSize = 100

func init() {
	resource.AddTestSweepers("kion_label", &resource.Sweeper{
		Name: "kion_label",
		F:    sweepLabel,
	})
}

// sweepLabel paginates GetLabelIndex collecting ids whose test resources
// carry the acceptance-test prefix, then deletes them via DeleteLabel.
func sweepLabel(_ string) error {
	conn, err := conns.SharedClient()
	if err != nil {
		return fmt.Errorf("getting shared client: %w", err)
	}
	ctx := context.Background()

	var ids []int64
	page := int64(1)
	for {
		out, err := conn.Client.GetLabelIndex(ctx, generated.GetLabelIndexParams{
			Page:  generated.NewOptInt64(page),
			Count: generated.NewOptInt64(int64(sweepPageSize)),
		})
		if err != nil {
			return fmt.Errorf("listing kion_label: %w", err)
		}
		resp, ok := out.(*generated.LabelListPaginatedResponse)
		if !ok || !resp.Data.Set {
			break
		}
		items := resp.Data.Value.Items
		if items.Set && !items.Null {
			for _, item := range items.Value {
				if item.ID.Set && sweepLabelMatch(item) {
					ids = append(ids, int64(item.ID.Value))
				}
			}
		}
		total := int64(0)
		if resp.Data.Value.Total.Set {
			total = resp.Data.Value.Total.Value
		}
		if total > 0 && page*int64(sweepPageSize) >= total {
			break
		}
		if !items.Set || items.Null || len(items.Value) == 0 {
			break
		}
		page++
	}

	for _, id := range ids {
		if _, err := conn.Client.DeleteLabel(ctx, generated.DeleteLabelParams{ID: id}); err != nil {
			return fmt.Errorf("deleting kion_label (%d): %w", id, err)
		}
	}
	return nil
}

func sweepLabelMatch(item generated.Label) bool {
	for _, s := range []string{
		item.Color,
		item.Key,
		item.Value,
	} {
		if strings.HasPrefix(s, "test-acc") {
			return true
		}
	}
	return false
}
