# Response scalars the provider exposes nowhere.
#
# Generated by: go test ./internal/kgen/fieldaudit/ -update
# Guarded by:   TestNoUnexposedResponseScalars
#
# Each entry is a field the API returns that is neither a resource attribute
# nor filterable on the data source. Most are a generator gap, not a decision:
# buildListDSData walks the resource's (settable) attributes, so server-owned
# fields are never candidates. Some entries are deliberate -- credentials must
# stay unexposed -- and those are marked so nobody "fixes" them.
#
# Reasons are preserved across regeneration; new entries arrive as TODO.

account.account_email: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
account.account_name: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
account.deleted_at: "generator gap: server-owned lifecycle field"
account_cache.key_id: "credential -- deliberate, must stay unexposed (pinned by TestCredentialFieldsStayUnexposed)"
account_cache.key_secret: "credential -- deliberate, must stay unexposed (pinned by TestCredentialFieldsStayUnexposed)"
account_linkage.azure_domain: "generator gap: server-owned field, absent from the create body"
account_linkage.azure_username: "generator gap: server-owned field, absent from the create body"
account_linkage.created_at: "generator gap: server-owned lifecycle field"
account_linkage.delete_finalized_at: "generator gap: server-owned lifecycle field"
account_linkage.deleted_at: "generator gap: server-owned lifecycle field"
account_linkage.updated_at: "generator gap: server-owned lifecycle field"
ami.unavailable_in_aws: "generator gap: server-owned field, absent from the create body"
app_api_key.created_at: "generator gap: server-owned lifecycle field"
app_api_key.last_used: "generator gap: server-owned lifecycle field"
app_api_key.user_id: "generator gap: server-owned field, absent from the create body"
azure_account.account_email: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
azure_account.account_number: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
azure_account.car_external_id: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
azure_account.created_at: "generator gap: server-owned lifecycle field"
azure_account.deleted_at: "generator gap: server-owned lifecycle field"
azure_account.include_linked_account_spend: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
azure_account.linked_account_number: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
azure_account.linked_role: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
azure_account.service_external_id: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
azure_account.use_org_account_info: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
azure_arm_template.created_by: "generator gap: server-owned lifecycle field"
azure_arm_template.ct_managed: "generator gap: provenance flag, server-owned so absent from the create body"
azure_arm_template.is_enabled: "generator gap: server-owned field, absent from the create body"
azure_arm_template.version: "generator gap: server-owned field, absent from the create body"
azure_policy.azure_managed_policy_def_id: "generator gap: provenance flag, server-owned so absent from the create body"
azure_policy.ct_managed: "generator gap: provenance flag, server-owned so absent from the create body"
azure_policy.enabled: "generator gap: server-owned field, absent from the create body"
azure_role.azure_managed_policy: "generator gap: provenance flag, server-owned so absent from the create body"
azure_role.car_restricted: "generator gap: server-owned field, absent from the create body"
azure_role.enabled: "generator gap: server-owned field, absent from the create body"
azure_role.system_managed_policy: "generator gap: provenance flag, server-owned so absent from the create body"
cft.system_managed: "generator gap: provenance flag, server-owned so absent from the create body"
compliance_check.default_severity_type_id: "generator gap: server-owned field, absent from the create body"
compliance_program.managed_resource_library_checksum: "generator gap: provenance flag, server-owned so absent from the create body"
compliance_program.managed_resource_library_id: "generator gap: provenance flag, server-owned so absent from the create body"
compliance_program.parent_program_id: "generator gap: server-owned field, absent from the create body"
compliance_program.system_managed_program: "generator gap: provenance flag, server-owned so absent from the create body"
compliance_standard.system_managed: "generator gap: provenance flag, server-owned so absent from the create body"
custom_account.account_email: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
custom_account.account_type_id: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
custom_account.car_external_id: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
custom_account.created_at: "generator gap: server-owned lifecycle field"
custom_account.deleted_at: "generator gap: server-owned lifecycle field"
custom_account.include_linked_account_spend: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
custom_account.linked_account_number: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
custom_account.linked_role: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
custom_account.service_external_id: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
custom_account.skip_access_checking: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
custom_account.use_org_account_info: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
custom_variable.created_by: "generator gap: server-owned lifecycle field"
custom_variable.default_value: "generator gap: server-owned field, absent from the create body"
gcp_account.account_email: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
gcp_account.account_number: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
gcp_account.car_external_id: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
gcp_account.created_at: "generator gap: server-owned lifecycle field"
gcp_account.deleted_at: "generator gap: server-owned lifecycle field"
gcp_account.include_linked_account_spend: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
gcp_account.linked_account_number: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
gcp_account.linked_role: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
gcp_account.service_external_id: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
gcp_account.use_org_account_info: "shared SDK Account struct -- may be inapplicable to this cloud; needs per-resource judgment"
gcp_iam_role.car_restricted: "generator gap: server-owned field, absent from the create body"
gcp_iam_role.gcp_id: "generator gap: server-owned field, absent from the create body"
gcp_iam_role.gcp_managed_policy: "generator gap: provenance flag, server-owned so absent from the create body"
gcp_iam_role.system_managed_policy: "generator gap: provenance flag, server-owned so absent from the create body"
scope.criteria_error: "generator gap: server-owned field, absent from the create body"
scope.identifier: "generator gap: server-owned field, absent from the create body"
service_control_policy.aws_managed_policy: "generator gap: provenance flag, server-owned so absent from the create body"
service_control_policy.created_by_user_id: "generator gap: server-owned lifecycle field"
service_control_policy.system_managed_policy: "generator gap: provenance flag, server-owned so absent from the create body"
user.created_at: "generator gap: server-owned lifecycle field"
user.deleted_at: "generator gap: server-owned lifecycle field"
user.display_name: "generator gap: server-owned field, absent from the create body"
user.enabled: "generator gap: server-owned field, absent from the create body"
user.last_login: "generator gap: server-owned lifecycle field"
user.password_needs_update: "generator gap: server-owned field, absent from the create body"
