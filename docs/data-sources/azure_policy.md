---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kion_azure_policy Data Source - kion"
subcategory: ""
description: |-
  Use this data source to access information about Kion AzurePolicys. Supports lookup by id (recommended) or by filter blocks (legacy).
---

# kion_azure_policy (Data Source)

Use this data source to access information about Kion AzurePolicys. Supports lookup by id (recommended) or by filter blocks (legacy).

## Example Usage

```terraform
data "kion_azure_policy" "example" {
  # Optional
  # id = 1

  # filter {
  #   name   = "example"
  #   regex  = false
  #   values = []
  # }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filter` (Block List) Filter results by attribute. Multiple filter blocks are AND'd; values within a single filter are OR'd. (see [below for nested schema](#nestedblock--filter))
- `id` (Number) The ID of a single azure_policy to fetch. Mutually exclusive with `filter` blocks.

### Read-Only

- `description` (String)
- `list` (Attributes List) All azure_policys matching the supplied id or filter blocks. (see [below for nested schema](#nestedatt--list))
- `name` (String)
- `parameters` (String)
- `policy` (String)

<a id="nestedblock--filter"></a>
### Nested Schema for `filter`

Required:

- `name` (String) The field name whose values you wish to filter by. Supports dotted paths (e.g. "owner.id") to traverse nested objects.
- `values` (List of String) The values of the field name you specified. A row matches if its field equals any of these values.

Optional:

- `regex` (Boolean) When true, the values are treated as regular expressions instead of exact matches.


<a id="nestedatt--list"></a>
### Nested Schema for `list`

Read-Only:

- `azure_managed_policy_def_id` (String)
- `ct_managed` (Boolean)
- `description` (String)
- `enabled` (Boolean)
- `id` (Number)
- `name` (String)
- `parameters` (String)
- `policy` (String)
