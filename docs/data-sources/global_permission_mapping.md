---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kion_global_permission_mapping Data Source - kion"
subcategory: ""
description: |-
  Use this data source to access information about Kion Global Permission Mappings.
---

# kion_global_permission_mapping (Data Source)

Use this data source to access information about Kion Global Permission Mappings.

## Example Usage

```terraform
data "kion_global_permission_mapping" "example" {
  # filter {
  #   name   = "example"
  #   regex  = false
  #   values = []
  # }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filter` (Block List) Filter results by attribute. Multiple filter blocks are AND'd; values within a single filter are OR'd. (see [below for nested schema](#nestedblock--filter))

### Read-Only

- `list` (Attributes List) List of permission mappings. (see [below for nested schema](#nestedatt--list))

<a id="nestedblock--filter"></a>
### Nested Schema for `filter`

Required:

- `name` (String) The field name whose values you wish to filter by. Supports dotted paths (e.g. "owner.id") to traverse nested objects.
- `values` (List of String) The values of the field name you specified. A row matches if its field equals any of these values.

Optional:

- `regex` (Boolean) When true, the values are treated as regular expressions instead of exact matches.


<a id="nestedatt--list"></a>
### Nested Schema for `list`

Read-Only:

- `app_role_id` (Number)
- `user_groups_ids` (Set of Number)
- `user_ids` (Set of Number)
