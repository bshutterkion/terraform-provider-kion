---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kion_billing_rule Resource - kion"
subcategory: ""
description: |-
  Manages a Kion Billing Rule.
---

# kion_billing_rule (Resource)

Manages a Kion Billing Rule.

## Example Usage

```terraform
resource "kion_billing_rule" "example" {
  # Optional
  # billing_source_ids = []
  # description        = "example"
  # end_month          = 1
  # name               = "example"
  # rule_type          = 1
  # rule_value         = 0.0
  # start_month        = 1
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `billing_source_ids` (Set of Number) List of Billing Source IDs attached to the billing rule.
- `description` (String) Description of the billing rule. The description can be up to 1024 characters long.
- `end_month` (Number) End month of the billing rule. This must be in the format YYYYMM where 202501 represents January 2025.
The end month is exclusive, e.g. if you want the rule to end in January 2025, send a value of 202502.
Omit if the end month should be "Never Ends". If included, the value must be after the start month.
- `name` (String) Name of the billing rule. The name can be up to 255 characters long.
- `rule_type` (Number) Type of the billing rule. The allowed values are:

1 - Rate Conversion
2 - Markup
3 - Discount
- `rule_value` (Number) The Value of the Billing Rule.

This is handled differently based on the Type of Billing Rules. For instance:

For a 5% markup, the value would be 5.0
For a 10% discount, the value would be 10.0
For a 1.214 currency conversion, the value would be 1.214
- `start_month` (Number) Start month of the billing rule. This must be in the format YYYYMM where 202501 represents January 2025.

### Read-Only

- `id` (String) Billing Rule ID.
