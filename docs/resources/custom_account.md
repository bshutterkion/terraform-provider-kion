---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kion_custom_account Resource - kion"
subcategory: ""
description: |-
  Attaches a custom account to a project in Kion. If project_id is provided, the account will be added to the corresponding project, otherwise the account will be added to the account cache. Once added, an account can be moved between projects or in and out of the account cache by changing the project_id. When importing existing Kion accounts into terraform state, you can use one of these methods:  Default import (tries project first, then cache): terraform import kion_custom_account.example 123 Explicit project account import: terraform import kion_custom_account.example account_id=123 Explicit cache account import: terraform import kion_custom_account.example account_cache_id=123
---

# kion_custom_account (Resource)

Attaches a custom account to a project in Kion. If project_id is provided, the account will be added to the corresponding project, otherwise the account will be added to the account cache. Once added, an account can be moved between projects or in and out of the account cache by changing the project_id. When importing existing Kion accounts into terraform state, you can use one of these methods:  Default import (tries project first, then cache): terraform import kion_custom_account.example 123 Explicit project account import: terraform import kion_custom_account.example account_id=123 Explicit cache account import: terraform import kion_custom_account.example account_cache_id=123

## Example Usage

```terraform
resource "kion_custom_account" "example" {
  # Required
  account_name   = "example"
  account_number = "example"
  payer_id       = 1
  project_id     = 1
  start_datecode = "example"

  # Optional
  # account_alias = "example"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `account_name` (String) The name of the account as it will appear in the application.
- `account_number` (String) The account number or identifier for the custom account.
- `payer_id` (Number) The ID of the payer to link the account to.
- `project_id` (Number) The ID of the project to link the account to.
- `start_datecode` (String) The start date that the account's spend accrues against the linked project.

### Optional

- `account_alias` (String) Alias of the account in the application.

### Read-Only

- `id` (String) Account ID.
