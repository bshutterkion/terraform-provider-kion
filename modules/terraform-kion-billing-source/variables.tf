# Generated by `kgen module` from the provider schema. Do not edit.

variable "aws_connection" {
  type    = object({ account_number = optional(string), bucket_access_role = optional(string), storage_bucket = optional(string), storage_prefix = optional(string), storage_region = optional(string) })
  default = null
}

variable "azure_connection" {
  description = "CustomBillingSourceAzureConnection defines the Azure storage connection for a custom billing source. Provide either credentialed_billing_source_id (reuse an existing Azure billing source's tenant credentials) or the tenant credential fields, not both."
  type        = object({ credentialed_billing_source_id = optional(number), storage_container = optional(string), storage_prefix = optional(string), storage_primary_endpoint = optional(string), tenant_app_id = optional(string), tenant_client_secret = optional(string), tenant_cloud_partition_id = optional(number), tenant_domain = optional(string) })
  default     = null
}

variable "billing_start_date" {
  description = "Start date of billing source (YYYY-MM)."
  type        = string
  default     = null
}

variable "name" {
  description = "Name of the billing source."
  type        = string
  default     = null
}

variable "skip_validation" {
  description = "When true, will skip validating the connection to defined bucket."
  type        = bool
  default     = null
}
