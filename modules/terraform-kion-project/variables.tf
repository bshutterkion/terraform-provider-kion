# Generated by `kgen module` from the provider schema. Do not edit.

variable "archived" {
  description = "Whether the project is archived."
  type        = bool
  default     = null
}

variable "auto_pay" {
  description = "True means the application can use the spend plan to process payments from the account. Should be true unless using a custom module."
  type        = bool
  default     = null
}

variable "budget" {
  description = "The project budget."
  type        = set(object({ amount = optional(number), data = optional(set(object({ amount = number, datecode = string, funding_source_id = optional(number), priority = optional(number) }))), end_datecode = string, funding_source_ids = optional(set(number)), start_datecode = string }))
  default     = null
}

variable "default_aws_region" {
  description = "Default AWS region that the project will use for assuming into accounts."
  type        = string
  default     = null
}

variable "description" {
  description = "Description for the project in the application."
  type        = string
  default     = null
}

variable "labels" {
  description = "The labels applied to the project."
  type        = map(string)
  default     = null
}

variable "move_ou_settings" {
  description = "Settings applied when moving the project between OUs."
  type        = set(object({ cloud_rule_setting = optional(string), financial_setting = optional(string) }))
  default     = null
}

variable "name" {
  description = "Name of the Project in the application."
  type        = string
}

variable "ou_id" {
  description = "ID of the OU containing the project."
  type        = number
}

variable "owner_user_group_ids" {
  description = "List of groups IDs who will own the project. Is required if no owner user IDs are listed."
  type        = list(number)
  default     = null
}

variable "owner_user_ids" {
  description = "List of user IDs who will own the project. Is required if no owner group IDs are listed."
  type        = list(number)
  default     = null
}

variable "permission_scheme_id" {
  description = "ID of the permission scheme applied to the project."
  type        = number
}

variable "project_funding" {
  type    = list(object({ amount = optional(number), end_datecode = optional(string), funding_order = optional(number), funding_source_id = optional(number), start_datecode = optional(string) }))
  default = null
}
