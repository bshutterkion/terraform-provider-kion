# Generated by `kgen module` from the provider schema. Do not edit.

variable "cloud_provider_policies" {
  description = "The provider-specific policies this automation policy runs. At least one is required."
  type        = list(object({ apply_to_all_regions = optional(bool), cloud_provider_id = optional(number), policy = optional(string), regions = optional(list(string)), resources = optional(list(string)) }))
}

variable "cloud_rule_ids" {
  description = "IDs of the cloud rules the policy is attached to."
  type        = set(number)
  default     = null
}

variable "description" {
  description = "Description of the automation policy."
  type        = string
  default     = null
}

variable "enabled" {
  description = "Whether the policy is enabled."
  type        = bool
  default     = null
}

variable "engine" {
  description = "Engine that runs the policy. 0 (Kion) is the only accepted value; the API defines 1 for Cloud Custodian but rejects it."
  type        = number
  default     = null
}

variable "name" {
  description = "Name of the automation policy. Must be unique."
  type        = string
}

variable "owner_user_group_ids" {
  description = "IDs of the user groups that own the policy."
  type        = set(number)
  default     = null
}

variable "owner_user_ids" {
  description = "IDs of the users who own the policy."
  type        = set(number)
  default     = null
}

variable "scheduled_frequency" {
  description = "When the policy runs. Omit for a policy that only runs on demand."
  type        = object({ days_of_month = optional(list(number)), days_of_week = optional(list(number)), end_date = optional(string), hour = optional(number), interval_hours = optional(number), minute = optional(number), quarterly_recurrence = optional(number), start_date = optional(string), time_zone_identifier = optional(string), type = optional(number), weekly_recurrence = optional(number) })
  default     = null
}
